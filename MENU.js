/*******************************************************
 *  📨 SENDER POSTMAN — MENU
 *  📅 Версия: 2025-11
 *  Файл: MENU.js
 *******************************************************/


/* ─────────────────────────────────────────────────────
 * 1. onOpen — создаёт меню "📨 Sender Postman"
 *    Пункты:
 *    1.1 Отправить новые отчёты
 *    1.2 Проверить новые отчёты (без отправки)
 *    1.3 Проверить новые ссылки (CSV-приглашения)
 *    1.4 Открыть лог SENDER (sender-log)
 *    1.5 Открыть лог LINKS (received-log)
 * ─────────────────────────────────────────────────────
 */
function onOpen() {
  const ui = SpreadsheetApp.getUi();
  const menu = ui.createMenu('📨 Sender Postman');

  // 1.1 Отправить новые отчёты
  menu.addItem('🚀 Отправить новые отчёты', 'senderPostmanRun');

  // 1.2 Проверить новые отчёты (без отправки)
  menu.addItem('🔍 Проверить новые отчёты (без отправки)', 'senderPostmanCheck');

  // 1.3 Проверить новые ссылки (CSV-приглашения)
  //    Использует нашу обёртку senderInviteCheck → senderInvite_('menu-check')
  menu.addItem('🔗 Проверить новые ссылки (CSV)', 'senderInviteCheck');

  menu.addSeparator();

  // 1.4 Открыть лог SENDER (лист "sender-log")
  menu.addItem('📊 Открыть лог SENDER', 'openSenderLog');

  // 1.5 Открыть лог LINKS (лист "received-log")
  menu.addItem('📥 Открыть лог LINKS', 'openReceivedLog');

  menu.addToUi();
}


/* ─────────────────────────────────────────────────────
 * 2. openSenderLog — переключает на лист sender-log
 * ─────────────────────────────────────────────────────
 */
function openSenderLog() {
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  const sh = ss.getSheetByName('sender-log');
  const ui = SpreadsheetApp.getUi();

  if (sh) {
    ss.setActiveSheet(sh);
    ui.alert('📊 Лог SENDER открыт.');
  } else {
    ui.alert('⚠️ Лист "sender-log" ещё не создан.');
  }
}


/* ─────────────────────────────────────────────────────
 * 3. openReceivedLog — переключает на лист received-log
 * ─────────────────────────────────────────────────────
 */
function openReceivedLog() {
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  const sh = ss.getSheetByName('received-log');
  const ui = SpreadsheetApp.getUi();

  if (sh) {
    ss.setActiveSheet(sh);
    ui.alert('📥 Лог LINKS (received-log) открыт.');
  } else {
    ui.alert('⚠️ Лист \"received-log\" ещё не создан.');
  }
}


/* ─────────────────────────────────────────────────────
 * 4. senderInviteCheck — обёртка для меню
 *    (вызов основной функции ловца CSV с флажком 'menu-check')
 * ─────────────────────────────────────────────────────
 */
function senderInviteCheck() {
  senderInvite_('menu-check');
}